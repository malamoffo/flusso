<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- permessi già presenti nel tuo progetto -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- permessi media necessari per foreground service Android 14+ -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- permesso notifica media richiesto da Android 13+ -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:label="Flusso"
        android:icon="@mipmap/ic_launcher"
        ...>

        <!-- MainActivity già presente -->
        <activity android:name=".MainActivity" ...>
            ...
        </activity>

        <!-- FileProvider già presente, lascialo invariato -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- AndroidAutoService: aggiorna solo foregroundServiceType -->
        <service
            android:name=".AndroidAutoService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">

            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>

        <!-- MediaButtonReceiver per gestire i tasti hardware del media -->
        <receiver android:name="androidx.media.session.MediaButtonReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

        <!-- Dichiarazione Android Auto obbligatoria -->
        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />

    </application>
</manifest>